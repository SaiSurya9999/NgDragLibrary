import { NgDragDirective } from './ng-drag.directive';

describe('NgDragDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDragDirective();
    expect(directive).toBeTruthy();
  });
});
