import { DragBoundaryDirective } from './drag-boundary.directive';

describe('DragBoundaryDirective', () => {
  it('should create an instance', () => {
    const directive = new DragBoundaryDirective();
    expect(directive).toBeTruthy();
  });
});
